
/**
 * Application-wide configuration settings
 */
export const appConfig = {
  // Video settings
  video: {
    /**
     * Duration in milliseconds before redirecting from the video player
     * to the booking form
     */
    redirectDuration: 5000,
    
    /**
     * Whether to show video controls (play, pause, etc.)
     */
    showControls: true,
    
    /**
     * Whether videos should autoplay when opened
     */
    autoplay: true,
  }
};
